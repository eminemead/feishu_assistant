node_modules
.env
ai-context
dist
.vercel
.env*.local
bun.lock

# TypeScript build cache
.tsbuildinfo

# DSPyGround local data
.dspyground/

# bv (beads viewer) local config and caches
.bv/

# Log files
logs/
.mastra/

# Agent workspace (ephemeral scratch)
workspace/

# Agent state (durable per-thread, not version controlled)
state/

# Agent workspace and state (ephemeral/per-thread)
workspace/
state/
